# Docker Compose env — values injected into OME container environment
# Must match corresponding values in backend/secrets.env

OME_API_TOKEN=your-ome-api-token-here
OME_WEBHOOK_SECRET=your-ome-webhook-secret-here
